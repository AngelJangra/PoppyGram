import type {NextApiRequest,NextApiResponse} from 'next';import {db} from '../../lib/db';import {requireAdmin} from '../../lib/adminAuth';export default async function h(req:NextApiRequest,res:NextApiResponse){if(!requireAdmin(req,res))return;if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const phone=String(req.body?.phone||'');const enabled=Boolean(req.body?.enabled);const{error}=await db.from('accounts').update({ping_enabled:enabled,next_ping_at:enabled?new Date().toISOString():null}).eq('phone',phone);if(error)return res.status(500).json({error:error.message});res.json({success:true})}