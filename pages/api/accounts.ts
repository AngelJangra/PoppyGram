import type {NextApiRequest,NextApiResponse} from 'next';import {db} from '../../lib/db';import {requireAdmin} from '../../lib/adminAuth';export default async function h(req:NextApiRequest,res:NextApiResponse){if(!requireAdmin(req,res))return;const{data,error}=await db.from('accounts').select('phone,label,meta,status,ping_enabled,last_ping,next_ping_at,ping_interval_minutes,failure_count').order('created_at',{ascending:false});if(error)return res.status(500).json({error:error.message});res.json({accounts:data||[]})}