import type {NextApiRequest,NextApiResponse} from 'next';import {db} from '../../lib/db';import {requireAdmin} from '../../lib/adminAuth';export default async function h(req:NextApiRequest,res:NextApiResponse){if(!requireAdmin(req,res))return;if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const phone=String(req.body?.phone||'');if(!phone)return res.status(400).json({error:'Phone required'});const{error}=await db.from('accounts').delete().eq('phone',phone);if(error)return res.status(500).json({error:error.message});await db.from('audit_logs').insert({event:'account.removed',message:`Removed ${phone}`});res.json({success:true})}